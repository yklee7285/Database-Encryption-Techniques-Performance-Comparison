CREATE TABLE patients_app_encryption (
    patient_id INTEGER PRIMARY KEY,
    name VARCHAR(100),
    date_of_birth DATE,
    identification_card_no VARCHAR(255),
    medical_condition VARCHAR(255),
    prescription VARCHAR(255)
);
